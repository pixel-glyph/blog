# Site settings
title: i'd rather be a polymath
email: ninestein09@gmail.com
description: An eclectic web log for your inner Renaissance fanatic.
url: "http://pixel-glyph.github.io/blog" # the base hostname & protocol for your site
paginate: 3

# Build settings
markdown: kramdown
permalink: /:title

#Default Settings

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts" # previously `post` in Jekyll 2.2.
    values:
      author: Andrew JB
      layout: "post"
      category: conferences
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "pages" # previously `post` in Jekyll 2.2.
    values:
      layout: "page"